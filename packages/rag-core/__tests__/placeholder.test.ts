/**
 * Test suite placeholder for @nimbus/rag-core.
 *
 * Per rules.md §4.6: every pipeline stage must have at least one unit test
 * before it's considered done. Tests go here using Vitest.
 *
 * Structure (to be added as each stage is implemented):
 *   chunker.test.ts
 *   embedder.test.ts
 *   retriever.test.ts
 *   reranker.test.ts
 *   generator.test.ts
 */

import { describe, it, expect } from 'vitest';

describe('@nimbus/rag-core — placeholder', () => {
  it('test suite is wired up correctly', () => {
    // This test exists only to confirm Vitest can find and run tests in this package.
    // Delete this once real tests are added.
    expect(true).toBe(true);
  });
});
